{
  "name": "mindtouch",
  "version": "1.0.0",
  "private": true,
  "description": "MindTouch cloud API + admin dashboard (Vercel)",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
