services:
  - type: web
    name: mindtouch
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
    healthCheckPath: /api/health
